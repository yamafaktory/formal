[project]
name = "formal"
version = "1.0.0"
description = "An LLM-driven property checker for code, backed by Lean 4 as a proof engine."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "pydantic>=2.9.0",
]

[project.scripts]
formal = "formal.cli:main"

[dependency-groups]
dev = [
    "ruff>=0.4.0",
    "pytest>=8.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/formal"]

[tool.hatch.build.targets.wheel.force-include]
"lean_project/lakefile.toml" = "formal/_lean_project/lakefile.toml"
"lean_project/lean-toolchain" = "formal/_lean_project/lean-toolchain"
"lean_project/lake-manifest.json" = "formal/_lean_project/lake-manifest.json"
"lean_project/Warmup.lean" = "formal/_lean_project/Warmup.lean"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
# E: pycodestyle errors
# F: pyflakes (unused imports, undefined names)
# I: isort (import ordering)
# UP: pyupgrade (modernise syntax)

[tool.ruff.format]
quote-style = "double"
