#!/usr/bin/env bash
set -euo pipefail

DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

command -v uv >/dev/null 2>&1 || {
	echo "uv is required: https://docs.astral.sh/uv/getting-started/installation/" >&2
	exit 1
}

exec uv run --quiet --project "$DIR" formal "$@"
